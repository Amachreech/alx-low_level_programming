#include <stdio.h>

/**
 * main - Entry ommit some alpha
 *
 * Return: Always (Success)
 */
int main(void)
{
	char h;

	for (h = 'a'; h <= 'z'; h++)
	{
		if (h! = 'e' && h! = 'q')
		{
			putchar(h);
		}
	}

	putchar('\n');

	return(0);
}
