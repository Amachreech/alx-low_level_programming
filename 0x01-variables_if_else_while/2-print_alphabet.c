#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry
 *
 * Return: Always 0 (successful)
 */
int main(void)
{
	char h;

	for (h = 'a'; h <= 'z'; h++)
		putchar(h);
	putchar('\n')
	return (0);
}
